spring.application.name=SecDemoJulio

# OAuth client used by scripts/auth-smoke.sh
app.oauth.client-id=client
app.oauth.client-secret=secret

# Demo/testing only: expose the issued MFA code so the smoke script can complete the MFA flow
app.expose-mfa-code=true
